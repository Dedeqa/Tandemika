# Модуль 4 — Тесты (tests)

Тег: `#tandemika/tests` · Статус: ⚪ не начат

## Scope
Серверные таймеры (НЕ клиентские), идемпотентная отправка ответов,
автопроверка закрытых вопросов, очередь на ручную проверку открытых.

## Глубина (не голый CRUD)
- Таймеры на сервере: дедлайн считается сервером, защита от подкрутки времени клиентом.
- Идемпотентная отправка ответов (ключ идемпотентности, защита от дублей).
- Автопроверка закрытых + очередь ручной проверки открытых.

## Задачи
<!-- Появятся, когда дойдём до модуля -->
